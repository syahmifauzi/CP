#include <bits/stdc++.h>
#define rep(i, a, n) for (int i = a; i < n; ++i)
#define dbg(x) cout << (#x) << " is " << x << endl 
using namespace std;

int r, s;

int main()
{
  scanf("%d%d", &r, &s);
  printf("%d\n", 2 * s - r);

  return 0;
}
